apply plugin: 'com.android.library'

android {
    compileSdkVersion 27

    defaultConfig {
        minSdkVersion 27
    }

    sourceSets {
        main.manifest.srcFile 'AndroidManifest.xml'
    }
}
